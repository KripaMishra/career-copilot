schemaVersion: 1
id: s08-sparse-question
description: "S08: a sparse profile defers the save; no save, fetch, or report work happens and the owner gets one concise question instead of a fabricated success."
kind: contract
persona: P06
fixture: sparse-profile
stubs: [s08-model]
turns:
  - { id: t1, channel: telegram, input: { kind: text, text: "/save https://linkedin.com/jobs/view/1" }, actorId: "1001", conversationId: "telegram:2001", expected: accepted }
assertions:
  - A-TOOLS-EXACT
  - A-TOOL-CONTEXT
  - A-JOB-STATE
  - A-TRANSCRIPT-COMPLETE
  - A-BUDGET
  - A-CANARY-CONTAINED
  - A-AUTH-BEFORE-MODEL
  - A-LOG-ALLOWLIST
  - A-ISOLATED-FIXTURE
  - A-NO-UNHANDLED-ERROR
  - { id: A-JOB-STATE, path: "state.jobs", op: count, value: 0 }
  - { id: A-TOOLS-EXACT, path: "replies", op: count, value: 1 }
tools:
  forbid: [save-job, web_fetch]
  counts: { save-job: 0 }
limits: { maxTurns: 5, maxWallClockMs: 60000, maxModelCalls: 10 }
rubrics: [task_completion, tool_selection, conversational_quality]
